---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: givtcp-config
  namespace: home
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 30M
  storageClassName: nfs-fast-kubes
